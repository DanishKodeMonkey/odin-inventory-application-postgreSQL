<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('../partials/head.ejs') %>
    </head>
    <body>
        <header><%- include('../partials/header.ejs') %></header>
        <main class="d-flex mx-5 align-items-center justify-content-center">
            <div class="content">
                <h1><%= title %>: <%= item.name %></h1>
                <p><%= item.description %></p>
                <p><%= item.price %></p>
                <p><%= item.numberInStock %></p>
                <hr>
                <p>Do you really want to delete this item? This cannot be undone.</p>
                    
                <form method="post">
                    <div class="form-group">
                        <input type="hidden" id="itemid" class="form-control" name="itemid" value="<%= item._id %>">
                        <button class="btn btn-primary" type="submit">Delete</button>
                    </div>
                </form>
                 
            </div>
        </main>
        <footer><%- include('../partials/footer.ejs') %></footer>
    </body>
</html>
